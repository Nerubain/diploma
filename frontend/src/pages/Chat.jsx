import React from 'react';

import Layout from '@components/Layout';
import ChatContainer from '@components/Chat/BigChat/ChatContainer';

export default function Chat() {
  return (
    <Layout>
      <ChatContainer />
    </Layout>
  );
}
